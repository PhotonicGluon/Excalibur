---
slug: /
---

import { features } from "@site/src/pages/index";

# Welcome to Excalibur

Welcome to Excalibur, a trustless, secure file management solution using zero-trust security and enterprise-grade encryption.

## Features

<ul>
    {features.map((feature) => (
        <li><b>{feature.icon} {feature.title}</b>: <span dangerouslySetInnerHTML={{ __html: feature.description }} /></li>
    ))}
</ul>

## Quickstart

Ready to begin? Follow the [Quickstart guide](/docs/category/quickstart) to get started with Excalibur.
