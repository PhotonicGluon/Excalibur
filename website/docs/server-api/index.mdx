# Server API

Generally, the available API endpoints for an Excalibur server can be accessed via `/api/docs` (or `/api/redoc` for Redoc documentation). However, some processes for interacting with the server need further explanation. That is the purpose of these pages.
