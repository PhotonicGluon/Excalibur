import { SortValues } from "@lib/files/sorting";

/**
 * Preferences related to the file explorer.
 */
export type FileExplorerPreferenceValues = SortValues; // So far, no custom preferences for the file explorer
