Implemented [OPAQUE](https://datatracker.ietf.org/doc/html/rfc9807) protocol as an alternative to Secure Remote Password (SRP) for bilateral authentication and key exchange.

- New accounts will, by default, use OPAQUE instead of SRP.
