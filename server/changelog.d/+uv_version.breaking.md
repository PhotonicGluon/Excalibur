💥 We will now use `uv`'s `0.11.x` series to build and install the server package, updating the minimum version from `0.10.9` in `pyproject.toml` and GitHub actions to `0.11.16` ([#33](https://github.com/PhotonicGluon/Excalibur/pull/33))
