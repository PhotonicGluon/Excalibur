💥 The OPAQUE registration endpoint (`/api/auth/opaque/register`) now requires clients to encrypt a shared symmetric key using the server's public key and send it to the server

- Please check the updated documentation for more information
