## [v0.7.1](https://github.com/PhotonicGluon/Excalibur/tree/v0.7.1) - 2026-07-09

> [!NOTE]
> Version 0.7.0 was skipped due to misconfigured version fields. Version 0.7.1 contains no changes except for the version field.

### 🔒️ Security

- 🔒️ Addressed a security issue where the ExEF footer could have been left out when sending the request body and yet still accepted by the server, resulting in possibly tampered bodies being accepted by the server

- 🔒️ Changed the behaviour of encrypted routes so that they _only_ accept encrypted payloads (i.e., `X-Encrypted` must be set to `true`)
  - This prevents an attacker from 'downgrading' the encryption level of a route by replacing a legitimate encrypted payload with an attacker-controlled malicious plaintext
  - This setting can only be bypassed on debug mode (via the `--debug` flag)

- 🔒️ Fixed a security issue where the nonce in the Proof-of-Possession header is consumed before the validity of the header is checked
  - This could allow an attacker to flood the server with a bunch of nonces, exhausting the nonce cache

- 🔒️ Removed `encrypted` query parameter from the directory changes listener endpoint (`/api/files/listen`)
  - All communications over this endpoint are now encrypted by default

### 💥 Breaking Changes

- 💥🔒️ Include query parameters in the Proof-of-Possession (PoP) header calculation
  - This does _not_ apply to WebSocket endpoints, as their PoP header is includes as one of the query parameters
- 💥 Uses of the account creation key (ACK) have been replaced with the server's account creation public key
- 💥 Changed the Proof of Possession (PoP) header name from `X-SRP-PoP` to `X-Auth-PoP`
- 💥 Changed vault file organization to be sorted by UUID instead of by usernames
- 💥 Replaced the security details endpoint (`/api/users/security/{username}`) with an authentication info endpoint (`/api/auth/info/{username}`)
  - The authentication info endpoint only returns the authentication protocol
- 💥 Combined the vault key endpoint (`/api/users/vault`) and 'additional info' endpoints (`/api/users/info/{username}` and `/api/users/edit-info/{username}`) into a single vault info endpoint (`/api/users/vault`)
  - This endpoint returns the account unlock key (AUK) key generation function, the AUK salt, the encrypted vault key, and the 'additional info'
  - Editing the AUK key generation function and the 'additional info' is done by sending a `PUT` request to that endpoint
- 💥 The OPAQUE login endpoint (`/api/auth/opaque`)'s behaviour has changed:
  - It will no longer respond with `ERR: User does not exist` if the user does not exist. Instead, a fake user vector will be returned. Clients are expected to detect this and respond accordingly
  - The final authentication token is now sent as an ExEF message instead of a 3-part JSON object

  Please read the updated documentation for more information.

- 💥 The OPAQUE registration endpoint (`/api/auth/opaque/register`) now requires clients to negotiate a shared session key with the server using the Noise-NK protocol before continuing with OPAQUE registration
  - Please check the updated documentation for more information
- 💥 The check user endpoint (`/api/user/check/{username}`) is now only accessible in debug mode

### ✨ New Features

- 🗃️ A fake user will be created upon server start
- ✨ Added a new WebSocket endpoint (`/api/auth/opaque/edit-record`) for changing the OPAQUE record of a user
  - This encompasses both changing the username and password of a user
  - This also allows changing of the saved key generation function on the server
    - This is just a record of _what_ key generation function was used to generate the user's keys

### 🔄 Changes

- 🔒️ Changed the JWT signing key from a per-run randomly generated key to a static value in `config.toml`
- 🔒️ Tightened the timestamp Proof-of-Possession (PoP) validity window to be half of the previous window
- ⚰️ Removed redundant `username` path parameter and `404 Not Found` response code from `/api/users/vault/{username}` (now is `/api/users/vault`)
- ⚰️ Removed the `security.srp` table from the Excalibur configuration
- 🔨 Added `--validate-config` and `--silent` flags to the `excalibur config generate-keys` command
- 🗃️ Modified the `User` table:
  - Added new `id` and `keygen_algorithm` fields
  - Changed the primary key of the table from `username` to `id`
  - Renamed the `additional_info` field to `vault_info`
- 🗃️ Removed SRP-related fields from the `User` table
- 🗃️ Updated DuckDB from 1.4 Andium to 1.5 Variegata
- 🛂 Changed subject (`sub`) of the authentication token to be the user's ID instead of the username
- 💄 Enhanced the look and feel of the `excalibur backup` command
- 🗑️ Removed `comms_uuid` query parameter from the OPAQUE registration endpoint (`/auth/opaque/register`)
- 🗑️ Removed `srp_salt` from the security details returned by the security details endpoint (`/api/auth/info`)
- 🗑️ Removed SRP login endpoint (`/api/auth`) in favour of OPAQUE-3DH login
- 🗑️ Removed add user endpoint using SRP (`/api/user/add/{username}`) in favour of OPAQUE-3DH user creation
- 🗑️ Removed the SRP group size endpoint (`/api/auth/group-size`) as SRP is no longer supported

### 🐛 Bug Fixes

- 🐛 Fixed a bug where the server was not actually obeying the `X-Content-Type` header
- 🐛 Fixed a bug where invalid JWT tokens that are missing required fields would cause the server to throw `500 Internal Server Error`
- 🐛 Fixed a bug where `PersistentTTLCache`'s `pop()` method does not update the local disk copy of the cache

### ⬆️ Dependencies

- ➕ Added `httpx2` test dependency
- ➖ Removed `httpx` test dependency
- ⬆️ Updated `duckdb` from `1.4.4` to `1.5.3`
- ⬆️ Updated `fastapi` from `0.136.3` to `0.139.0` ([#63](https://github.com/PhotonicGluon/Excalibur/pull/63), [#68](https://github.com/PhotonicGluon/Excalibur/pull/68), [#72](https://github.com/PhotonicGluon/Excalibur/pull/72), [#88](https://github.com/PhotonicGluon/Excalibur/pull/88))
- ⬆️ Updated `ipython` development dependency from `9.14.1` to `9.15.0` ([#86](https://github.com/PhotonicGluon/Excalibur/pull/86))
- ⬆️ Updated `pydantic-settings` from `2.14.1` to `2.14.2` ([#78](https://github.com/PhotonicGluon/Excalibur/pull/78))
- ⬆️ Updated `pytest` test dependency from `9.0.3` to `9.1.1` ([#66](https://github.com/PhotonicGluon/Excalibur/pull/66), [#74](https://github.com/PhotonicGluon/Excalibur/pull/74))
- ⬆️ Updated `ruff` development dependency from `0.15.16` to `0.15.20` ([#61](https://github.com/PhotonicGluon/Excalibur/pull/61), [#76](https://github.com/PhotonicGluon/Excalibur/pull/76), [#91](https://github.com/PhotonicGluon/Excalibur/pull/91))
- ⬆️ Updated `sqlmodel` from `0.0.38` to `0.0.39` ([#82](https://github.com/PhotonicGluon/Excalibur/pull/82))

### 🧹 Miscellaneous

- 💥 We will now use `uv`'s `0.11.x` series to build and install the server package, updating the minimum version from `0.10.9` in `pyproject.toml` and GitHub actions to `0.11.26`
- 🧑‍💻 Added additional safeguards and warnings when using debug mode (`--debug`)
- 🔒️ Changed point multiplication algorithm of `Ristretto255` from double-and-add to Montgomery ladder
- 🔨 Updated the configuration migration strategy used by `excalibur config update`
